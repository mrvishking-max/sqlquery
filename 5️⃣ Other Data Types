5️⃣ Other Data Types

BOOLEAN → true/false

ENUM → predefined set of values ('small','medium','large')

JSON → store JSON data (MySQL/PostgreSQL)

Example:
CREATE TABLE OtherDemo (
    is_active BOOLEAN,
    size ENUM('Small','Medium','Large'),
    details JSON
);

INSERT INTO OtherDemo VALUES (TRUE, 'Medium', '{"course":"SQL","duration":30}');
SELECT * FROM OtherDemo;